name: Veri Temizleyici & Otomatik İyileştirme (Sanitizer)

on:
  schedule:
    - cron: '0 4 * * *' # Her gün 07:00 TR saati
  workflow_dispatch: # Manuel tetikleme izni

jobs:
  maintenance:
    name: Sytem Maintenance & Data Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: 🧹 Run Campaign Sanitizer (Genel Temizlik)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/campaign_sanitizer.ts

      - name: 🤖 Run AI Auto-Correction (Eksik Metin & Hata Düzeltme)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          GOOGLE_GEMINI_KEY: ${{ secrets.GOOGLE_GEMINI_KEY }}
        run: npx tsx scripts/auto_correct.ts

      - name: 📊 Fix Percentage Earning Format (Yüzde Formatı Düzeltme)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/fix_percentage_earnings.ts --execute
