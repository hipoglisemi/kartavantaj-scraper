name: 🔢 Matematik Denetimi

on:
  schedule:
    - cron: '0 3 * * 0' # Her Pazar 06:00 TSI
  workflow_dispatch: # Manuel çalıştırma izni

jobs:
  audit-math:
    name: Kademeli Harcama Matematiğini Denetle
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install Dependencies
        run: npm ci
      
      - name: 🔢 Run Tiered Math Fix Script
        run: npx tsx fix_tiered_math.ts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
