name: 🧹 Günlük Temizlik

on:
  schedule:
    - cron: '0 0 * * *'  # Her gün 03:00 TSI (00:00 UTC)
  workflow_dispatch:

jobs:
  cleanup:
    name: Arşivleme ve Temizlik
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install Dependencies
        run: npm ci
      
      - name: 🗑️ Archive Expired Campaigns
        run: npx tsx src/garbageCollector.ts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
