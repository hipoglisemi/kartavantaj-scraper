name: Periodic Scraper & Auto-Fix

on:
  schedule:
    # Her ayın 1, 8, 15 ve 22'sinde saat 00:01'de çalışır (UTC)
    # Türkiye saati ile (UTC+3) 03:01'e denk gelir.
    - cron: '1 0 1,8,15,22 * *'
  # Manuel tetiklemek isterseniz 'Run workflow' butonu için:
  workflow_dispatch:

jobs:
  scrape-and-fix:
    runs-on: ubuntu-latest
    
    # Maksimum çalışma süresi (Dakika). 
    # 5000 kampanya için artırılabilir, şimdilik 360 (6 saat) yeterli.
    timeout-minutes: 360

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: 🚀 Run Yapı Kredi Scraper (World, Adios, Play, Crystal)
        run: npx tsx src/scrapers/yapikredi.ts --ai
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          GOOGLE_GEMINI_KEY: ${{ secrets.GOOGLE_GEMINI_KEY }}

      - name: 🚀 Run Akbank Scraper (Axess, Free, Wings)
        run: npx tsx src/scrapers/akbank.ts --ai
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          GOOGLE_GEMINI_KEY: ${{ secrets.GOOGLE_GEMINI_KEY }}

      - name: 🚀 Run İş Bankası Scraper (Maximum)
        run: npx tsx src/scrapers/isbankasi.ts --ai
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          GOOGLE_GEMINI_KEY: ${{ secrets.GOOGLE_GEMINI_KEY }}

      - name: 🚀 Run Garanti BBVA Scraper (Bonus)
        run: npx tsx src/scrapers/garantibbva.ts --ai
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          GOOGLE_GEMINI_KEY: ${{ secrets.GOOGLE_GEMINI_KEY }}

      - name: 🚀 Run Halkbank Scraper (Paraf)
        run: npx tsx src/scrapers/halkbank.ts --ai
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          GOOGLE_GEMINI_KEY: ${{ secrets.GOOGLE_GEMINI_KEY }}

      - name: 🚀 Run VakıfBank Scraper (World)
        run: npx tsx src/scrapers/vakifbank.ts --ai
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          GOOGLE_GEMINI_KEY: ${{ secrets.GOOGLE_GEMINI_KEY }}

      - name: 🚀 Run Ziraat Bankası Scraper (Bankkart)
        run: npx tsx src/scrapers/ziraat.ts --ai
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          GOOGLE_GEMINI_KEY: ${{ secrets.GOOGLE_GEMINI_KEY }}

      # Gelecekte diğer bankaları buraya ekleyebilirsiniz
      # - name: 🚀 Run Maximum Scraper
      #   run: npx ts-node src/scrapers/maximum.ts --ai

      - name: 🔧 Run Auto-Fix Pipeline (Pass 1)
        run: npx tsx src/autoFix.ts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          GOOGLE_GEMINI_KEY: ${{ secrets.GOOGLE_GEMINI_KEY }}

      - name: 🔧 Run Auto-Fix Pipeline (Pass 2 - Double Check)
        # İlk geçişte düzelmeyen varsa tekrar dene (Tam kontrol)
        run: npx tsx src/autoFix.ts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          GOOGLE_GEMINI_KEY: ${{ secrets.GOOGLE_GEMINI_KEY }}

      - name: 🗑️ Run Garbage Collector
        run: npx tsx src/garbageCollector.ts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
